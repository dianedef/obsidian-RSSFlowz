import { beforeEach, afterEach } from 'vitest'
import sinon from 'sinon'

beforeEach(() => {
  // Configuration avant chaque test
})

afterEach(() => {
  // Restaure tous les stubs/mocks après chaque test
  sinon.restore()
}) 